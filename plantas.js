class Planta {
    constructor(costo, vida, daño) {
      this.costo = costo;
      this.vida = vida;
      this.daño = daño;
    }
  
    colocar(x, y) {
      // Código para colocar la planta en el tablero
    }
  
    atacar() {
      // Lógica para el ataque de la planta
    }
  }
  
  // Puedes añadir diferentes tipos de plantas aquí
  